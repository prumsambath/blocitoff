<%= form_for [list, Item.new], remote: true do |f| %>
  <div class="input-group">
    <%= f.text_field :name, class: 'form-control', placeholder: 'Enter Item' %>
    <span class="input-group-btn">
      <%= f.submit 'New Item', class: 'btn btn-default' %>
    </span>
  </div>
<% end %>
