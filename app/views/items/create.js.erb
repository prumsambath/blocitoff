$('#items').prepend('<%= j render partial: 'items/item', locals: { list: @list, item: @item } %>');
$('#incomplete-item-count').html('<%= @list.incomplete_items.count %>');

$('#new_item').html("<%= j render partial: 'items/form', locals: { list: @list } %>")
